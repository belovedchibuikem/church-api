# Known Gaps

- Local MySQL 8.4 connectivity, the complete fresh migration set, and an isolated logical backup/restore drill are verified. Production MySQL backup/restore, recovery objectives, live Redis, production mail, live S3, and a live PSP remain unverified. Do not treat this host as production-ready.
- The default local and authoritative test MySQL databases have the complete migration set applied. The last recorded integrated suite result is 333 tests and 2,020 assertions (Church/Mission/security-review batch). Additional member-completeness and press-download tests exist; this documentation pass did not re-run the full suite, so those totals are not updated.
- Browser registration/login/logout, signed verification, generic password recovery, CSRF/session transport, mobile credential rotation/reuse detection, MFA challenges/recovery, and protected HTTP middleware are implemented. Production mail delivery, credential-risk telemetry, MFA recovery support procedures, and live browser/mobile E2E remain.
- Canonical Person/Profile and optional User linkage now exist, but the legacy `users.name` field remains until registration/profile contracts are approved and migrated.
- Permission bundles, role/scope persistence, append-only decisions, global/geography/exact/own-record containment, initial Admin policies, and protected enforcement are implemented. Delegation/separation-of-duties and domain-specific record policies remain OD-006+ gates.
- Transport-neutral foundations exist across all named domains. Church/Home Church, Mission, remaining-domain Admin catalog/read plus mutation write routes are wired, including KCA curriculum, events create, mission invitations, communication templates, file approval, local catalog search, and advisory AI request envelopes. Advisory AI still uses the disabled provider; search is local LIKE catalog matching, not an external index.
- Member routes now include event registration ticket read (`GET /user/events/registrations/{registration}`), membership list/start (`GET /user/memberships`, `POST /user/churches/{church}/memberships`), Home Church dashboard (`GET /user/home-churches/{homeChurch}`), Home Church reports (`POST /user/home-churches/{homeChurch}/reports`), public Press download (`GET /press/publications/{publicId}/download` as an octet-stream), member file list/upload (`GET`/`POST /user/files`), and own-record file stream (`GET /user/files/{file}` plus admin `GET /admin/platform/files/{file}/content`). Streaming is limited to Available assets the caller may see; there is no general signed-URL CDN contract.
- Stable error envelopes cover framework validation, authentication, authorization, not-found, method, rate-limit, service, and unexpected failures. Domain-specific conflict/workflow mappings will be added with their owning modules.
- Authoritative OpenAPI and deterministic TypeScript/Dart clients cover 21 public operations and 192 protected identity/User/Admin operations (213 contracted). Protected mutations use `ProtectedDomainRecord` plus specific input schemas rather than untyped `JsonObject`/`SuccessEnvelope` payloads. Adjacent apps: web imports the generated TypeScript client via `frontend/apps/web/lib/generated-api.ts` (health uses it; other public catalogue calls still wrap `public-api.ts`); mobile path-depends on `family_house_connect_public_api`; web admin overlays dispatch mutations to Laravel through `executeAdminAction`. Code-complete client↔API wiring is not production-ready.
- The optional S3-compatible core and adapter are installed and contract-tested, but no live bucket/credentials have been supplied. Queue/scheduler configuration, structured logging, CI, monitoring guidance, and local restore have evidence; mail and production infrastructure do not.
- Redis is configured for production-style cache/queue use and the CI workflow supplies Redis plus `phpredis`, but this machine has neither a Redis service nor the PHP Redis extension. The hosted CI workflow and production Redis readiness remain unverified.
- The local restore drill left a 61 KB plaintext development dump at `C:\Users\BELOVED\AppData\Local\Temp\fhc-restore-verify-20260825-2300\family_house_connect.sql` because the execution sandbox denied file deletion. The isolated restore database was removed. Delete the dump when host policy permits.
- Optional storage administration is implemented behind global permissions, recent MFA, SSRF controls, mutation throttling, and immutable audit events. No live S3-compatible bucket/credentials have been supplied, so the real provider probe, least-privilege policy, encryption/lifecycle settings, and restore behavior remain an OD-012 deployment gate.
- Audit/access-decision immutability is enforced and global-only minimized review APIs require recent MFA plus explicit permissions. Production database credentials still need restricted table privileges, retention controls, and backup validation.
- Identity/profile factories exist for tests; no operational people, users, or audit events are seeded because these are not reference data.
- File assets retain provider, logical disk, location revision, and object key. Switching local/S3 affects only new writes; referenced S3 locations cannot be changed until an explicit asset migration exists. Uploads remain quarantined pending malware scan; an Admin approval action can mark a quarantined/pending asset available. Derivatives, a real scanner adapter, and S3 signed CDN delivery remain. Public Press download and own-record file streams use `OpenFileAssetStreamQuery` against Available objects.
- Platform configuration and flags have protected, scoped, audited Admin APIs, but no operational keys/defaults are seeded because provider/ministry policy remains unresolved. Flag evaluation is not authorization.
- Organization/geography, Church/Home Church, Mission, platform settings/storage, security audit review, remaining-domain global catalog, remaining-domain mutations, local catalog search, and advisory request APIs now exist. Gates remain domain authority, OD-006 through OD-010, and provider availability. Registered mutation permissions may still be denied by record/workflow/governance policies.
- Payment providers, outbound messaging providers, search indexes, and advisory AI providers are not selected. Payments without a live provider, webhook verification, outbound delivery, and disabled AI remain fail-closed. Giving intents exist; default `PAYMENT_GOVERNANCE_MODE=deny` returns clear 422 `PAYMENT_GOVERNANCE_DENIED` until OD-009/010. Optional `allow_local` + `local_manual` is QA-only (see repo `docs/HONEST_LIMITS.md`). Search returns local catalog matches. Alert evaluation/visibility policies now allow authenticated scoped operators.
- Member KCA curriculum **reads** (`/user/kca/*`) are implemented. KCA certification is eligible only when every assignment is in final assessment; thresholds/fees/signer authority and evidence writes remain OD-008 gates. Payment webhook/provider behavior and outbound messaging/AI providers remain OD-009 gates.
- Safeguarding restricted reads and guardian/child decisions remain default deny pending OD-006 and OD-007. Privacy export execution is allowed; deletion/anonymization/legal-hold workflows are not implemented.
- Data-export artifact code is present and its schema alteration is ordered after `data_subject_requests`; the combined schema now passes a fresh migration. Broader privacy lifecycle policy/E2E evidence remains pending.
- Alert and export focused tests now pass inside the integrated MySQL suite; privacy deletion/anonymization remains denied by design while export execution is allowed.
- Next.js and Flutter consume generated clients as noted above. No cross-repository release pipeline, production mail, live Redis-on-this-host, or browser/mobile E2E journey is proven.
- The Next.js handoff DOCX was text-extracted, but visual rendering could not run because LibreOffice is unavailable on this machine.
- Public Home Church submission safely creates a canonical Person/profile without accepting a confidential Person identifier, and identical retries do not duplicate identity. A new idempotency key for the same human can still create another Person because no approved unauthenticated identity-resolution/contact-matching policy exists; this must be resolved before cross-key deduplication is claimed.
